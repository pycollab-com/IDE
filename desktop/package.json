{
  "name": "pycollab-ide-desktop",
  "productName": "PyCollab IDE",
  "version": "0.1.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:mac": "electron-packager . \"PyCollab IDE\" --platform=darwin --arch=arm64 --out=release --overwrite --app-bundle-id=com.pycollab.ide --icon=assets/PyCollabIDE.icns --extra-resource=../server --extra-resource=../client/dist"
  },
  "devDependencies": {
    "@electron/packager": "^18.4.4",
    "electron": "^37.2.6"
  }
}
