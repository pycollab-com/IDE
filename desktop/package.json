{
  "name": "pycollab-ide-desktop",
  "productName": "PyCollab IDE",
  "version": "0.1.1",
  "private": true,
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:mac": "electron-packager . \"PyCollab IDE\" --platform=darwin --arch=arm64 --out=release --overwrite --prune=true --ignore='^/node_modules($|/)' --ignore='^/release($|/)' --app-bundle-id=com.pycollab.ide --icon=assets/PyCollabIDE.icns --extra-resource=../server --extra-resource=../client/dist"
  },
  "devDependencies": {
    "@electron/packager": "^18.4.4",
    "electron": "^37.2.6"
  }
}
